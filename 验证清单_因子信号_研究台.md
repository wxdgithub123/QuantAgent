# 验证清单：因子信号 & 研究台

> 生成日期：2026-06-06  
> 依据文档：`量化研究平台产品需求文档（PRD）.docx`、`量化研究平台 · 第二阶段 PRD.docx`、`功能模块.xlsx`  
> 服务地址：后端 `http://localhost:8002` / 前端 `http://localhost:3002`

---

## 一、因子信号（Factor Signal）模块

### 1.1 顶部控制栏（上下文选择）

| # | 功能名称 | 验收标准 | 验证方法 | 预期结果 | 当前状态 |
|---|---------|---------|---------|---------|---------|
| 1.1.1 | 交易对选择器 | 下拉菜单包含 BTCUSDT / ETHUSDT / SOLUSDT / BNBUSDT / DOGEUSDT，选择后页面数据刷新 | 打开 `http://localhost:3002/signals`，在上下文控制栏切换交易对，观察下方卡片和表格数据变化 | 切换后因子快照表和信号事件表显示对应交易对数据；无数据时显示"暂无数据"而非报错 | ✅ 已实现 |
| 1.1.2 | K线周期选择器 | 下拉菜单包含 1m / 5m / 15m / 1h / 4h / 1d，选择后表格数据按周期过滤 | 在上下文控制栏切换周期为 `1h` → `1d`，观察因子快照表数据变化 | 因子和信号数据按所选周期过滤；切换后立即生效 | ✅ 已实现 |
| 1.1.3 | as_of_time 回看时间输入 | 输入如 `2026-05-28 09:30` 的格式后点击"应用"，仅展示 available_time ≤ 该时间的数据 | 输入一个历史时间点 → 点击"应用"按钮 → 观察表格数据变化 | 数据范围缩小到该时间之前；左上角 Badge 变为"按历史时间回看"；无数据时不报错 | ✅ 已实现 |
| 1.1.4 | 清除回看时间 | 点击"清除"按钮恢复到最新数据（实时） | 点击 1.1.3 的"清除"按钮 → 观察 Badge 恢复为"当前最新状态" | 数据恢复到最新，as_of_time 过滤解除 | ✅ 已实现 |
| 1.1.5 | 时间格式错误处理 | 输入非法格式（如乱码）后点击"应用"，给出友好提示 | 输入 `abc` → 点击"应用" | 页面顶部出现红色错误提示"时间格式无法识别"，不会白屏 | ✅ 已实现 |

**验证命令（后端 PIT 过滤）：**
```bash
# 实时数据
curl -s "http://localhost:8002/api/v1/signals/factors?symbol=BTCUSDT&interval=1h&limit=3" | python -m json.tool

# 历史时间点过滤（仅返回该时间之前可用的数据）
curl -s "http://localhost:8002/api/v1/signals/factors?symbol=BTCUSDT&interval=1h&as_of_time=2026-05-10T00:00:00&limit=3" | python -m json.tool
```
预期：第二个命令返回的数据条数 ≤ 第一个。

---

### 1.2 因子总览卡片

| # | 功能名称 | 验收标准 | 验证方法 | 预期结果 | 当前状态 |
|---|---------|---------|---------|---------|---------|
| 1.2.1 | 因子种类卡片 | 显示当前上下文下不同因子名称的数量 | 打开 `/signals`，查看第一行卡片组中"因子种类"卡片 | 数字 = `COUNT(DISTINCT factor_name)` 的结果（如 32） | ✅ 已实现 |
| 1.2.2 | 因子快照卡片 | 显示当前上下文下因子快照总记录数 | 查看"因子快照"卡片 | 数字 = 总记录行数（如 4,640） | ✅ 已实现 |
| 1.2.3 | 信号事件卡片 | 显示当前上下文下信号事件总数 | 查看"信号事件"卡片 | 数字 = 总信号数（如 1,201） | ✅ 已实现 |
| 1.2.4 | 近7天信号卡片 | 显示最近 7 天的信号数量 | 查看"近7天信号"卡片 | 数字 ≥ 0，不出现 NaN 或 undefined | ✅ 已实现 |
| 1.2.5 | 交易对卡片 | 显示有信号的交易对数量 | 查看"交易对"卡片 | 数字 = 不同 symbol 数 | ✅ 已实现 |
| 1.2.6 | 空数据状态 | 无数据时卡片显示 0 而非报错 | 未运行管道的交易对/周期组合（如 DOGEUSDT 的 1m） | 卡片显示 `0`，不白屏不报错 | ✅ 已实现 |

**验证命令：**
```bash
curl -s "http://localhost:8002/api/v1/signals/summary" | python -m json.tool
```
预期返回 `factor_total`、`distinct_factor_count`、`event_total`、`recent_7d` 等字段。

---

### 1.3 信号总览卡片（新增功能）

| # | 功能名称 | 验收标准 | 验证方法 | 预期结果 | 当前状态 |
|---|---------|---------|---------|---------|---------|
| 1.3.1 | 信号总数卡片 | 显示当前 symbol + interval 下的信号总数，细分 BUY/SELL/WAIT | 打开 `/signals`，查看上下文控制栏下方绿色边框卡片组第一张 | 格式如"1201 信号总数 / 23 买入 / 33 卖出 / 1145 观望" | ✅ 已实现 |
| 1.3.2 | 因子快照卡片 | 显示因子快照总数和不同因子种类数 | 查看青色边框卡片 | 如"4640 因子快照 / 32 种不同因子" | ✅ 已实现 |
| 1.3.3 | 活跃策略卡片 | 显示产生过信号的策略数量 | 查看紫色边框卡片 | 如"5 不同策略产生过信号" | ✅ 已实现 |
| 1.3.4 | 最新信号卡片 | 显示最近一次信号触发时间 | 查看琥珀色边框卡片 | 显示具体时间或"暂无" | ✅ 已实现 |
| 1.3.5 | 回看模式卡片 | 显示当前是实时模式还是历史回看模式 | 查看蓝色边框卡片 | 实时显示"实时"，回看显示"历史回看" | ✅ 已实现 |
| 1.3.6 | 最近强信号列表 | 显示最近 5 条 BUY/SELL 信号，点击可查看详情 | 查看信号总览卡片下方的 Badge 列表 | 每个 Badge 显示信号类型、策略、置信度和时间；点击弹出详情 | ✅ 已实现 |
| 1.3.7 | 空数据状态 | 无信号数据时卡片优雅降级 | 选择无数据的交易对/周期（如未跑过管道的组合） | 卡片显示 0，不报错不白屏；最近强信号区域隐藏 | ✅ 已实现 |

**验证命令：**
```bash
# 带上下文的信号总览
curl -s "http://localhost:8002/api/v1/signals/overview?symbol=BTCUSDT&interval=1h" | python -m json.tool

# 带历史时间过滤
curl -s "http://localhost:8002/api/v1/signals/overview?symbol=BTCUSDT&interval=1h&as_of_time=2026-05-10T00:00:00" | python -m json.tool
```
预期返回 `total_signals`、`by_signal_type`（含 BUY/SELL/WAIT 计数）、`recent_strong_signals`（最近 5 条强信号）。

---

### 1.4 因子快照表

| # | 功能名称 | 验收标准 | 验证方法 | 预期结果 | 当前状态 |
|---|---------|---------|---------|---------|---------|
| 1.4.1 | 因子快照表格 | 展示因子值表格，含交易对、因子名、类型、值、计算来源、上游来源、时间 | 打开 `/signals` → 切换到"因子快照"标签 | 表格有 7 列：交易对、因子、类型、值、计算来源、上游来源、时间 | ✅ 已实现 |
| 1.4.2 | 交易对过滤 | 输入框输入 BTCUSDT 后点击筛选 | 在过滤区输入 `BTCUSDT` → 点击"筛选" | 表格只显示 BTCUSDT 的因子快照 | ✅ 已实现 |
| 1.4.3 | 因子名过滤 | 输入框输入 sma_10 后点击筛选 | 输入 `sma_10` → 点击"筛选" | 表格只显示 sma_10 因子行 | ✅ 已实现 |
| 1.4.4 | 因子值格式化 | 大数值正确格式化，非数值显示 "-" | 查看 sma_* 行和 news_sentiment 行的 value 列 | 数值有千分位分隔；null 值显示 "-" | ✅ 已实现 |
| 1.4.5 | 因子中文名显示 | 因子定义表关联后显示 display_name | 查看因子列 | sma_10 → "10周期简单均线"；boll_mid → "布林带中轨" | ✅ 已实现 |
| 1.4.6 | 来源标签 | 计算来源和上游来源正确翻译 | 查看来源列 | `l5_pipeline` → "因子与策略信号管道"；`indicators` → "内部指标计算" | ✅ 已实现 |
| 1.4.7 | as_of_time 过滤 | 在上下文控制栏设置回看时间后，表格只显示该时间之前可用的数据 | 设置上下文 as_of_time → 切换到因子快照标签查看 | 数据时间戳 ≤ as_of_time；无数据时显示"暂无因子快照数据" | ✅ 已实现 |
| 1.4.8 | 空数据状态 | 无因子数据时优雅展示 | 选择未跑过管道的交易对 | 表格显示"暂无因子快照数据"，不报错 | ✅ 已实现 |

**验证命令：**
```bash
# 基础查询
curl -s "http://localhost:8002/api/v1/signals/factors?symbol=BTCUSDT&factor_name=sma_10&limit=3" | python -m json.tool

# PIT 过滤
curl -s "http://localhost:8002/api/v1/signals/factors?symbol=BTCUSDT&interval=1h&as_of_time=2026-05-05T00:00:00&limit=3" | python -m json.tool

# 因子时序（用于时间轴图表）
curl -s "http://localhost:8002/api/v1/signals/factors/BTCUSDT/sma_10/series?limit=100" | python -m json.tool
```

---

### 1.5 信号事件表

| # | 功能名称 | 验收标准 | 验证方法 | 预期结果 | 当前状态 |
|---|---------|---------|---------|---------|---------|
| 1.5.1 | 信号事件表格 | 展示信号列表，含交易对、信号类型(Badge)、置信度、策略、时间、详情按钮 | 切换到"信号事件"标签 | 表格有 6 列；BUY 绿色 Badge、SELL 红色 Badge、WAIT 灰色 Badge | ✅ 已实现 |
| 1.5.2 | Symbol 过滤 | 输入框过滤交易对 | 输入 `ETHUSDT` → 点击"筛选" | 表格只显示 ETHUSDT 信号（若无则显示空） | ✅ 已实现 |
| 1.5.3 | 信号类型过滤 | 下拉菜单选择 BUY/SELL/WAIT/全部 | 选择 `BUY` → 点击"筛选" | 只显示 BUY 信号 | ✅ 已实现 |
| 1.5.4 | 信号详情面板 | 点击"查看详情"展示完整信号链路信息 | 点击任意一条信号的"查看详情"按钮 | 展开详情面板，显示 signalId、symbol、信号类型、强度/置信度、触发时间、as_of_time、available_time、策略来源、触发条件、关联因子列表、关联决策 ID、OrderIntent ID、回测 ID、审计 ID | ✅ 已实现 |
| 1.5.5 | 关联因子展示 | 详情面板列出触发该信号的所有因子名和取值 | 查看详情面板的"相关因子"Badge 区域 | 显示多个蓝色因子 Badge | ✅ 已实现 |
| 1.5.6 | 因子取值 JSON | 可展开查看完整因子键值对 | 点击详情面板底部"因子取值 JSON"折叠区 | 展开显示格式化的 JSON | ✅ 已实现 |
| 1.5.7 | 关联链路跳转 | 关联的决策、回测、审计 ID 可跳转 | 查看详情面板的"关联决策"/"OrderIntent"/"回测"/"审计"字段 | ID 存在时显示可跳转链接（若对应页面已实现） | ⚠️ 部分需对应页面已实现 |
| 1.5.8 | 空数据状态 | 无信号时优雅展示 | 选择无数据的过滤条件 | 表格显示"暂无信号事件数据"，详情区显示"点击上方'查看详情'"引导文案 | ✅ 已实现 |

**验证命令：**
```bash
# 信号列表
curl -s "http://localhost:8002/api/v1/signals/events?symbol=BTCUSDT&interval=1h&limit=3" | python -m json.tool

# 信号类型过滤
curl -s "http://localhost:8002/api/v1/signals/events?symbol=BTCUSDT&signal_type=BUY&limit=3" | python -m json.tool

# 信号详情（替换 {signal_id} 为实际 ID）
curl -s "http://localhost:8002/api/v1/signals/events/1/detail" | python -m json.tool
```

---

### 1.6 因子/信号时间轴（可选）

| # | 功能名称 | 验收标准 | 验证方法 | 预期结果 | 当前状态 |
|---|---------|---------|---------|---------|---------|
| 1.6.1 | 因子时序图 | 输入 symbol 和 factor_name 后展示折线图 | 切换到"因子时序图"标签 → 输入 `BTCUSDT` / `sma_10` → 点击"查询" | 显示 sma_10 最近 100 个数据点的折线图 | ✅ 已实现 |
| 1.6.2 | 空数据状态 | 无数据时显示占位图 | 输入不存在的 factor_name → 点击"查询" | 显示"暂无数据"的图标和文字提示 | ✅ 已实现 |

---

### 1.7 因子字典 / 策略体系

| # | 功能名称 | 验收标准 | 验证方法 | 预期结果 | 当前状态 |
|---|---------|---------|---------|---------|---------|
| 1.7.1 | 因子字典卡片 | 展示每种因子的定义、计算说明、数据来源、实际快照数 | 切换到"因子字典"标签 | 每个卡片显示 display_name、factor_name、category Badge、快照数 | ✅ 已实现 |
| 1.7.2 | 因子研究资产目录 | 展示因子的目录视图，含可用性状态（可用/部分可用/暂不可用） | 切换到"研究资产目录"标签 | 因子卡片含可用性 Badge（绿色=可用、琥珀色=部分可用、灰色=暂不可用） | ✅ 已实现 |
| 1.7.3 | 目录筛选 | 支持按分类、可用性、搜索、是否参与回测/Agent 筛选 | 使用目录顶部的筛选控件 | 筛选条件改变后卡片列表即时更新 | ✅ 已实现 |
| 1.7.4 | 策略体系卡片 | 展示每个策略的使用因子、触发信号、风控规则、执行模式 | 切换到"策略体系"标签 | 每张卡片含策略 ID、类型、因子列表、信号列表、回测次数 | ✅ 已实现 |

**验证命令：**
```bash
curl -s "http://localhost:8002/api/v1/signals/factor-definitions" | python -m json.tool
curl -s "http://localhost:8002/api/v1/signals/factor-catalog" | python -m json.tool
curl -s "http://localhost:8002/api/v1/signals/strategies" | python -m json.tool
```

---

### 1.8 汇总统计

| # | 功能名称 | 验收标准 | 验证方法 | 预期结果 | 当前状态 |
|---|---------|---------|---------|---------|---------|
| 1.8.1 | 饼图：因子类型分布 | 按分类展示因子类型占比饼图 | 切换到"汇总统计"标签 | 显示饼图，各类别带颜色和图例 | ✅ 已实现 |
| 1.8.2 | 饼图：信号类型分布 | 按 BUY/SELL/WAIT 展示信号占比 | 查看第二个饼图 | BUY 绿色、SELL 红色、WAIT 灰色 | ✅ 已实现 |
| 1.8.3 | 柱状图：策略来源分布 | 按策略展示信号数量柱状图 | 查看柱状图 | X 轴为策略名，Y 轴为信号数量 | ✅ 已实现 |

---

## 二、研究台（Research Dashboard）模块

### 2.1 顶部控制栏

| # | 功能名称 | 验收标准 | 验证方法 | 预期结果 | 当前状态 |
|---|---------|---------|---------|---------|---------|
| 2.1.1 | 交易对选择器 | 下拉菜单选择后页面所有面板刷新 | 打开 `http://localhost:3002/dashboard`，切换交易对 | K线图、价格卡片、研究快照等全部更新 | ✅ 已实现 |
| 2.1.2 | WebSocket 连接状态 | 实时显示 WS 连接状态（实时/连接中/断线） | 查看顶部控制栏右侧的 WS 状态 Badge | 正常情况下绿色+动画圆点+显示"实时"；断线时红色 | ✅ 已实现 |
| 2.1.3 | K线周期切换 | 图表右上角可切换不同周期 | 点击图表右上角下拉菜单切换 `1d` → `1h` | K线图自动刷新为新周期数据 | ✅ 已实现 |

---

### 2.2 行情面板

| # | 功能名称 | 验收标准 | 验证方法 | 预期结果 | 当前状态 |
|---|---------|---------|---------|---------|---------|
| 2.2.1 | K 线图 | TradingView 风格 K 线图，支持缩放和鼠标悬停 | 打开 `/dashboard`，观察第一个大区域 | 显示当前交易对的 K 线图，可鼠标拖动缩放 | ✅ 已实现 |
| 2.2.2 | 最新价 | 实时显示当前价格（WebSocket 推送） | 查看"价格"卡片 | 显示 $79,xxx.xx 格式，随行情更新 | ✅ 已实现 |
| 2.2.3 | 24h 涨跌幅 | 显示涨跌百分比和绝对变化额 | 查看"24h 涨跌幅"卡片 | 正数绿色+TrendingUp 图标；负数红色+TrendingDown | ✅ 已实现 |
| 2.2.4 | 24h 成交量 | 显示成交量（USDT 计价） | 查看"24h 成交量"卡片 | 带千分位分隔的大数字 | ✅ 已实现 |
| 2.2.5 | 24h 最高 | 显示 24 小时最高价（🆕） | 查看"24h 最高"卡片（青色边框，ArrowUp 图标） | 显示 $xx,xxx.xx 格式价格 | ✅ 已实现 |
| 2.2.6 | 24h 最低 | 显示 24 小时最低价（🆕） | 查看"24h 最低"卡片（粉色边框，ArrowDown 图标） | 显示 $xx,xxx.xx 格式价格 | ✅ 已实现 |
| 2.2.7 | 数据 Provider | 明确显示行情数据来源（🆕） | 查看"数据 Provider"卡片（绿色边框，Globe 图标） | 显示 `OpenBB / yfinance` + `CCXT/OKX 备源` | ✅ 已实现 |
| 2.2.8 | 数据新鲜度 | 实时指示数据是否新鲜（🆕） | 查看"数据新鲜度"卡片（绿色/琥珀色圆点） | WS 连接时绿色脉冲圆点+"实时数据"；断线时琥珀色+"数据可能延迟" | ✅ 已实现 |
| 2.2.9 | K 线周期 | 显示当前选择的 K 线周期 | 查看"K 线周期"卡片 | 显示"1小时" / "1天" 等中文标签 | ✅ 已实现 |
| 2.2.10 | 报价来源对照 | 展示行情网关 vs 交易所直接报价的对比 | 查看"真实来源对照"卡片 | 显示两列：行情网关价格 / CCXT/OKX 价格 / 价差百分比 | ✅ 已实现 |
| 2.2.11 | 行情网关不可用 | 交易所报价获取失败时优雅降级 | 断开外部网络后刷新页面 | 价差列显示"交易所直接报价暂不可用"，不报错不白屏 | ✅ 已实现 |

---

### 2.3 市场快照卡片

| # | 功能名称 | 验收标准 | 验证方法 | 预期结果 | 当前状态 |
|---|---------|---------|---------|---------|---------|
| 2.3.1 | CoinGecko 热门币种 | 展示 CoinGecko Trending API 返回的热门币种 | 滚动到"热门币种"区域 | 显示 4 张币种卡片，含图标、名称、排名 | ✅ 已实现 |
| 2.3.2 | CoinGecko 不可用 | API 不可用时降级提示 | CoinGecko 限流或断网 | 显示"CoinGecko Trending 暂不可用......"琥珀色提示，不显示假数据 | ✅ 已实现 |

---

### 2.4 新闻 / 宏观面板

| # | 功能名称 | 验收标准 | 验证方法 | 预期结果 | 当前状态 |
|---|---------|---------|---------|---------|---------|
| 2.4.1 | 宏观指标面板 | 显示 CPI、利率、失业率、GDP、M2 等宏观指标 | 查看左侧"宏观指标"卡片 | 11 项指标，各显示数值+单位，来源于 OpenBB/FRED/OECD | ✅ 已实现 |
| 2.4.2 | 加密新闻面板 | 显示标准化后的新闻列表，含标题、来源、时间、外部链接 | 查看右侧"加密新闻"卡片 | 新闻列表带 OpenBB/yfinance + DuckDB 标签 | ✅ 已实现 |
| 2.4.3 | 新闻管道统计 | 显示已入库新闻数 | 查看新闻面板 Badge | `{n}条` 格式 | ✅ 已实现 |
| 2.4.4 | 空数据状态 | 无新闻/宏观数据时降级提示 | 首次启动时可能无数据 | 显示"暂无新闻记录。系统不会用示例新闻占位"，不白屏 | ✅ 已实现 |

---

### 2.5 AnalysisContext 面板（研究快照）

| # | 功能名称 | 验收标准 | 验证方法 | 预期结果 | 当前状态 |
|---|---------|---------|---------|---------|---------|
| 2.5.1 | 研究快照标题栏 | 展示快照模式（最新 / 历史回看）和加载状态 | 滚动到"研究台：TradingAgents 的统一研究材料"卡片 | 显示研究快照 Badge + 模式 Badge | ✅ 已实现 |
| 2.5.2 | 回看时间输入 | 输入历史时间后点击"按该时间回看"还原当时的系统状态 | 输入 `2026-05-10 09:30` → 点击按钮 | 所有面板（行情/因子/信号/新闻/Agent）统一截止到该时间点 | ✅ 已实现 |
| 2.5.3 | 全局时间轴 | 展示当前页面的 as_of_time 和对齐规则 | 查看快照内的时间轴区域 | 显示回看时间和"只使用该时间点之前已经可用的数据"规则说明 | ✅ 已实现 |
| 2.5.4 | 数据量概览 | 6 项数字卡片（K线/因子/信号/新闻/宏观/Agent角色） | 查看快照顶部的 6 列数字 | 每项显示具体数字+说明 | ✅ 已实现 |
| 2.5.5 | 行情面板（快照内） | 展示收盘价、成交量、VWAP、窗口 VWAP + 最近 K 线明细表 | 查看快照内"行情面板"区域 | 4 个数值卡片 + 明细表（时间/收盘价/成交量/VWAP） + Data Provider / 最新Bar时间 / 数据源标签（🆕） | ✅ 已实现 |
| 2.5.6 | 因子面板（快照内） | 按技术/情绪/宏观三类展示因子 | 查看"因子面板" | 三类因子分开展示，每个因子显示中文名和数值；底部有宏观指标标签 | ✅ 已实现 |
| 2.5.7 | 信号面板（快照内） | 展示最新强信号（BUY/SELL）和策略覆盖状态 | 查看"信号面板" | 两个区域：最新强信号（去重后）+ 每个策略最新状态 | ✅ 已实现 |
| 2.5.8 | TradingAgents 分析面板 | 展示最近一次决策的最终信号、置信度、风控状态和角色分析卡片 | 查看"TradingAgents 分析面板" | 顶部汇总卡片（BUY/SELL/WAIT + 置信度 + 风控） + 下方角色卡片网格 | ✅ 已实现 |
| 2.5.9 | 新闻面板（快照内） | 展示已标准化新闻，含情绪评分和资产映射 | 查看"新闻面板" | 新闻卡片含标题、来源、时间、情绪分数 Badge、资产映射标签 | ✅ 已实现 |
| 2.5.10 | 来源与一致性说明 | 解释 K 线、因子、信号、新闻的数据链路 | 查看"来源与一致性说明"区域 | 4 段文字说明数据来源和回看规则 | ✅ 已实现 |
| 2.5.11 | 加载状态 | 数据加载中显示加载动画和说明文案 | 切换交易对或设置回看时间时 | 显示"正在整理上下文"+ 旋转动画 | ✅ 已实现 |
| 2.5.12 | 错误降级 | 后端异常时显示友好提示 | 后端服务重启中时访问 | 琥珀色提示"研究快照暂不可用"，不白屏 | ✅ 已实现 |
| 2.5.13 | 无决策记录 | 无历史决策时显示引导文案 | 刚启动无决策记录时查看 Agent 面板 | 显示"当前时间点之前还没有 TradingAgents 决策记录。可以先在决策中心手动运行一次" | ✅ 已实现 |

**验证命令（研究快照 API）：**
```bash
# 最新快照
curl -s "http://localhost:8002/api/v1/market/research-snapshot/BTCUSDT?interval=1h" | python -m json.tool | head -80

# 历史时间点快照
curl -s "http://localhost:8002/api/v1/market/research-snapshot/BTCUSDT?interval=1h&as_of_time=2026-05-10T09:30:00" | python -m json.tool | head -80
```
预期：返回 `symbol`、`interval`、`as_of_time`、`mode`、`counts`、`bar_panel`、`factor_panel`、`signal_panel`、`news_panel`、`tradingagents_panel`、`global_time_axis` 等字段。

---

### 2.6 AnalysisContext API（给 TradingAgents 的输入）

| # | 功能名称 | 验收标准 | 验证方法 | 预期结果 | 当前状态 |
|---|---------|---------|---------|---------|---------|
| 2.6.1 | Context 组装 | 按 symbol + interval + as_of_time 组装完整 AnalysisContext | 调用下方 curl | 返回 bars、factors、signals、news、macro 五个维度的数据 | ✅ 已实现 |
| 2.6.2 | PIT 过滤 | 只包含 available_time ≤ as_of_time 的数据 | 传入历史 as_of_time 对比 | 历史时间点的数据量 ≤ 最新的 | ✅ 已实现 |

**验证命令：**
```bash
curl -s "http://localhost:8002/api/v1/signals/context/BTCUSDT?interval=1h" | python -m json.tool | head -40
```

---

## 三、联动测试（跨模块跳转）

| # | 功能名称 | 验收标准 | 验证方法 | 预期结果 | 当前状态 |
|---|---------|---------|---------|---------|---------|
| 3.1 | 信号 → 研究台 | 从信号详情中可关联到研究台对应时间点 | 在信号详情中点击关联决策 ID | 跳转到研究台或审计台 | ⚠️ 依赖对应页面 |
| 3.2 | 研究台 → 信号页 | 从研究快照的信号面板可看到信号来源 | 在研究台信号面板点击信号 | 查看信号详情、关联回测/审计 | ⚠️ 需前端进一步联调 |
| 3.3 | 导航栏切换 | 顶部导航可在研究台和因子信号之间切换 | 点击顶部导航的"研究台"和"因子/信号" | 页面正常切换，状态不丢失 | ✅ 已实现 |

---

## 四、边界情况与异常处理总览

| # | 场景 | 预期行为 | 涉及模块 |
|---|------|---------|---------|
| B1 | 数据库无数据（新部署） | 所有表格显示"暂无数据"，卡片显示 0，不白屏不报错 | 因子信号、研究台 |
| B2 | 后端 500 错误 | 前端显示琥珀色降级提示，展示缓存数据或"数据暂不可用" | 因子信号、研究台 |
| B3 | 网络超时 | 12s 超时后显示降级提示，页面不卡死 | 因子信号、研究台 |
| B4 | WebSocket 断线 | 价格卡片保留最后数据，新鲜度指示变琥珀色，自动重连 | 研究台 |
| B5 | as_of_time 格式错误 | 红色错误提示"时间格式无法识别"，不清空已有数据 | 因子信号 |
| B6 | 不存在的交易对 | API 返回空数据，前端显示"暂无数据" | 因子信号、研究台 |
| B7 | 管道未运行（无因子/信号） | overview 返回 0，summary 返回空对象 {} | 因子信号 |
| B8 | factor_definitions 表为空 | 因子字典/快照表使用 FACTOR_BLUEPRINTS 兜底，仍可分类显示 | 因子信号 |
| B9 | 后端重启中 | 前端等待+重试（最多3次），超时后降级提示 | 研究台 |

---

## 五、快速冒烟测试（5 分钟）

```bash
# 1. 后端健康
curl -s http://localhost:8002/health | python -m json.tool

# 2. 运行因子信号管道（如果还没数据）
curl -s -X POST "http://localhost:8002/api/v1/signals/run" \
  -H "Content-Type: application/json" \
  -d '{"symbol":"BTCUSDT","interval":"1h","limit":100,"strategies":["ma","rsi","boll","macd"],"include_wait_signals":true}' \
  | python -c "import json,sys; d=json.load(sys.stdin); print(f'Status: {d[\"status\"]}, Factors: {d.get(\"factor_rows_written\",0)}, Signals: {d.get(\"signal_rows_written\",0)}')"

# 3. 验证数据
echo "--- Summary ---"
curl -s "http://localhost:8002/api/v1/signals/summary" | python -c "import json,sys; d=json.load(sys.stdin); print(f'Factors: {d[\"factor_total\"]}, Events: {d[\"event_total\"]}')"

echo "--- Overview ---"
curl -s "http://localhost:8002/api/v1/signals/overview?symbol=BTCUSDT&interval=1h" | python -c "import json,sys; d=json.load(sys.stdin); print(f'Signals: {d[\"total_signals\"]}, Types: {d[\"by_signal_type\"]}')"

echo "--- Research Snapshot ---"
curl -s "http://localhost:8002/api/v1/market/research-snapshot/BTCUSDT?interval=1h" | python -c "import json,sys; d=json.load(sys.stdin); print(f'Bars: {d.get(\"counts\",{}).get(\"bars\",0)}, Factors: {d.get(\"counts\",{}).get(\"factors\",0)}, Signals: {d.get(\"counts\",{}).get(\"signals\",0)}')"

# 4. 打开前端
echo "--- Frontend ---"
echo "打开浏览器访问: http://localhost:3002/signals   (因子信号)"
echo "打开浏览器访问: http://localhost:3002/dashboard (研究台)"
```
