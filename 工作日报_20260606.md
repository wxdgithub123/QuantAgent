# 工作日报 — 2026年6月6日

## 一、今日工作概述

- **负责模块**：因子信号、研究台
- **一句话总结**：完成本地环境搭建与项目启动，修复数据库表结构缺失问题，按PRD规范和`功能模块.xlsx`需求完成因子信号和研究台两个模块的前后端功能增强，跑通因子信号管道并生成验证清单。

---

## 二、完成的功能清单

### 2.1 因子信号模块

| 子功能 | 状态 | 备注 |
|--------|------|------|
| 顶部控制栏（上下文选择） | ✅ 已完成 | 新增交易对选择器、K线周期选择器、as_of_time 回看时间输入/应用/清除 |
| 因子总览卡片 | ✅ 已有（集成） | 因子种类、因子快照、信号事件、近7天信号、交易对 5 张卡片 |
| 信号总览卡片 | ✅ 新增 | 信号总数（含BUY/SELL/WAIT细分）、因子快照、活跃策略、最新信号时间、回看模式 5 张卡片 + 最近强信号列表 |
| 因子快照表 | ✅ 已增强 | 新增 as_of_time 过滤、interval 过滤，表格含中文名和来源标签 |
| 信号事件表 | ✅ 已有（增强） | 新增 as_of_time/interval 过滤，详情面板含完整信号链路 |
| 因子/信号时间轴 | ✅ 已有 | 因子时序折线图（可选功能） |
| 因子字典 | ✅ 已有 | 25条因子定义，含计算说明和数据来源 |
| 因子研究资产目录 | ✅ 已有 | 含可用性状态、回测/Agent 反向链接 |
| 策略体系 | ✅ 已有 | 策略-因子-信号关系展示 |
| 汇总统计 | ✅ 已有 | 饼图（因子类型分布、信号类型分布）+ 柱状图（策略来源分布） |

### 2.2 研究台模块

| 子功能 | 状态 | 备注 |
|--------|------|------|
| 顶部控制栏 | ✅ 已有（集成） | 交易对选择器、WebSocket 状态、K线周期切换 |
| 行情面板 — K线图 | ✅ 已有 | TradingView 风格图表 |
| 行情面板 — 最新价/涨跌幅/成交量 | ✅ 已有 | WebSocket 实时推送 |
| 行情面板 — 24h最高/最低 | ✅ 新增 | 两张新卡片，含 ArrowUp/ArrowDown 图标 |
| 行情面板 — 数据 Provider | ✅ 新增 | 显示 OpenBB/yfinance 主源 + CCXT/OKX 备源 |
| 行情面板 — 数据新鲜度 | ✅ 新增 | WS 连接状态指示灯，实时/延迟双态 |
| 行情面板 — 报价来源对照 | ✅ 已有 | 行情网关 vs 交易所直接报价对比 |
| 市场快照卡片 | ✅ 已有 | CoinGecko 热门币种（含降级处理） |
| 新闻/宏观面板 | ✅ 已有 | 11项宏观指标 + 加密新闻列表（含情绪评分） |
| AnalysisContext 面板 | ✅ 已增强 | 完整的因子面板/信号面板/TradingAgents分析面板/新闻面板/全局时间轴，新增 Data Provider 标签、最新Bar时间、数据源显示 |
| 模拟交易面板 | ✅ 已有 | 虚拟账户、持仓、订单、风控状态 |

### 2.3 基础设施/数据库

| 工作项 | 状态 | 备注 |
|--------|------|------|
| `factor_definitions` 表创建 | ✅ 已完成 | 新建表，预填25条因子定义（覆盖行情基础/技术指标/波动率/新闻事件/宏观/加密特有6大类） |
| `factor_snapshots` 表补列 | ✅ 已完成 | 新增 `interval`、`as_of_time`、`instrument_id`、`event_time` 列 |
| `signal_events` 表补列 | ✅ 已完成 | 新增 `instrument_id` 列 |
| 因子信号管道测试 | ✅ 已完成 | 运行成功：BTCUSDT/1h → 4,640 因子行 + 1,201 信号行（ma/rsi/boll/macd/news_sentiment） |
| `.env` 配置修复 | ✅ 已完成 | 复制到 backend/ 目录确保 pydantic-settings 正确加载 |

---

## 三、验证结论

- **因子信号模块**：后端 API 全部可用（9个端点），前端页面正常渲染，管道数据成功入库
- **研究台模块**：后端 API 全部可用，前端页面正常渲染，WebSocket 实时数据推送正常
- **主要发现与修复**：
  1. `factor_definitions` 表不存在 → 新建并预填数据
  2. `factor_snapshots` 缺少 `interval`、`as_of_time`、`instrument_id`、`event_time` 列 → ALTER TABLE 补充
  3. `signal_events` 缺少 `instrument_id` 列 → ALTER TABLE 补充
  4. `.env` 文件不在 backend 工作目录内 → 复制到 backend/
  5. 多个僵尸 Python 进程占用端口 8002 → 强制终止后重启
  6. `/api/v1/signals/summary` 端点因编辑操作被意外覆盖 → 已恢复

---

## 四、接口变更说明

### 新增 API

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/v1/signals/overview` | 上下文感知的信号总览，支持 `symbol`、`interval`、`as_of_time` 参数 |

### 修改的 API

| 方法 | 路径 | 变更内容 |
|------|------|----------|
| `GET` | `/api/v1/signals/factors` | 新增 `as_of_time`、`interval` 可选参数，支持 PIT 过滤 |
| `GET` | `/api/v1/signals/events` | 新增 `as_of_time`、`interval` 可选参数，支持 PIT 过滤 |

### 前端路由（无新增，增强现有页面）

| 路由 | 页面 | 变更内容 |
|------|------|----------|
| `/signals` | 因子信号页 | 新增上下文控制栏、信号总览卡片区、最近强信号列表 |
| `/dashboard` | 研究台/Dashboard | 新增24h最高/最低卡片、数据Provider卡片、数据新鲜度卡片、行情面板内Data Provider/最新Bar时间标签 |

### 数据库变更

| 表 | 操作 | 字段 |
|----|------|------|
| `factor_definitions` | 新建 | 全表（id, factor_name, display_name, category, family, description, calculation, upstream_data, provider_hint, unit, default_interval, sort_order, is_active, created_at, updated_at） |
| `factor_snapshots` | ALTER TABLE | + `interval` VARCHAR(10), + `as_of_time` TIMESTAMPTZ, + `instrument_id` VARCHAR(255), + `event_time` TIMESTAMPTZ |
| `signal_events` | ALTER TABLE | + `instrument_id` VARCHAR(255) |
| `paper_trades` | ALTER TABLE | + `exchange_id` VARCHAR(50) DEFAULT 'okx'（修复 migration 006 断裂链） |
| `paper_positions` | ALTER TABLE | + `exchange_id` VARCHAR(50) DEFAULT 'okx' |

### 迁移修复

| 问题 | 修复方式 |
|------|---------|
| migration 006 的 `down_revision` 指向孤立分支 `d4293f8c131f`，导致从未执行 | 新建 migration `013_supplement_missing_columns.py` 覆盖所有缺失列 |
| `alembic_version` 表存储了过时标识符 `010_add_coordination_debate_fields`（文件内实际是 `"010"`） | 修正为 `"010"` → 升级到 `"013"` |
| 多分支 head 冲突（001-005 / d4293f8c131f-013 / 两个2026_04_08分支） | 用 `alembic upgrade 013` 指定目标版本 |

---

## 五、已知遗留问题

| # | 问题描述 | 影响范围 | 后续计划 |
|---|---------|---------|---------|
| 1 | ~~`paper_trades` 缺少 `exchange_id`~~ → ✅ 已解决 | — | 新增 migration 013 + ALTER TABLE |
| 2 | 因子信号管道目前仅对 BTCUSDT 运行过，ETHUSDT/SOLUSDT 等无 K 线数据（需要数据摄入先拉取） | 其他交易对页面显示为空 | 需配置数据摄入覆盖更多交易对 |
| 3 | 信号详情 → 研究台/审计台的联动跳转部分依赖对应页面是否已实现 | 跨模块跳转 | 等审计台/回测台完成后补联调 |
| 4 | 前端 Next.js 16 强制 Turbopack，Windows 下偶发 "failed to create whole tree" 错误 | 前端启动 | 已通过清理 `.next` 缓存绕过，建议关注 Next.js 更新 |
| 5 | ~~`factor_snapshots` / `signal_events` 新增列未写入迁移文件~~ → ✅ 已解决 | — | migration 013 已生成并执行 |
| 6 | migration 006 `down_revision` 指向孤立分支 `d4293f8c131f`，导致其从未被执行 | 新环境 `alembic upgrade head` 可能失败（多 head） | 已通过 migration 013 覆盖缺失列 + 用 `alembic upgrade 013` 指定目标版本解决 |

---

## 附录：服务运行状态

| 服务 | 地址 | 状态 |
|------|------|------|
| PostgreSQL | `localhost:5435` | ✅ 正常 |
| Redis | `localhost:6382` | ✅ 正常 |
| 后端 API | `http://localhost:8002` | ✅ 正常 |
| API 文档 | `http://localhost:8002/docs` | ✅ 可访问 |
| 前端 | `http://localhost:3002` | ✅ 正常 |
| 因子信号页 | `http://localhost:3002/signals` | ✅ 正常（含 4,640 因子 + 1,201 信号） |
| 研究台 | `http://localhost:3002/dashboard` | ✅ 正常 |
